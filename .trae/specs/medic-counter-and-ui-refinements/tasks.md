# Tasks

- [x] Task 1: Revert Player Info Panels
  - [x] SubTask 1.1: In `kurashScoreBoard.vue`, change Player 1 info box (left) back to `bg-white/10 backdrop-blur-md` (remove `bg-[#006600]`).
  - [x] SubTask 1.2: In `kurashScoreBoard.vue`, change Player 2 info box (right) back to `bg-white/10 backdrop-blur-md` (remove `bg-[#003366]`).

- [x] Task 2: Update Medic Indicator Colors
  - [x] SubTask 2.1: Locate P1 Medic indicators (Line ~83). Change `border-red-600` to `border-green-600` (or 500).
  - [x] SubTask 2.2: Change P1 active class `bg-red-600` to `bg-green-600`. Change shadow color to green.
  - [x] SubTask 2.3: Locate P2 Medic indicators (Line ~112). Change `border-red-600` to `border-blue-600` (or cyan-500 to match player theme).
  - [x] SubTask 2.4: Change P2 active class `bg-red-600` to `bg-blue-600`. Change shadow color to blue.

# Task Dependencies
- Tasks are independent.
