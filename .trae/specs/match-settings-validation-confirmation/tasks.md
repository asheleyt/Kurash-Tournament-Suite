# Tasks

- [x] Task 1: Implement name validation logic in `refereeController.vue`.
    - Create `isValidName` helper.
    - Update Name input fields with validation classes and error messages.
- [x] Task 2: Create Confirmation Modal component/template in `refereeController.vue`.
    - Design a modal that overlays the screen.
    - Display all match data (P1/P2 details, Gender, Category).
- [x] Task 3: Wire up the confirmation logic.
    - "Update Match" button -> Opens Modal.
    - Modal "Confirm" -> Calls `applyMatchSettings` and closes modal.
    - Modal "Cancel" -> Closes modal.
