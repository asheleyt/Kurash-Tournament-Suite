# Tasks

- [x] Task 1: Update Medic Pop-up Theme
  - [x] Modify `kurashScoreBoard.vue` in the `v-if="isMedicMode"` section.
  - [x] Apply conditional styling based on `timerPlayer`:
    - [x] `timerPlayer === 'player1'`: Border Green, Text Green.
    - [x] `timerPlayer === 'player2'`: Border Cyan/Blue, Text Cyan/Blue.
    - [x] Fallback (if any): Keep Red or default to a neutral color.

- [x] Task 2: Widen Header Panels
  - [x] Modify `kurashScoreBoard.vue` header section.
  - [x] Update Gender container: Change `min-w-[150px]` to `min-w-[280px]` (or similar).
  - [x] Update Weight Division container: Change `min-w-[150px]` to `min-w-[280px]` (or similar).
