# Tasks

- [x] Task 1: Update Reset Button Label
  - [x] Modify `refereeController.vue` to change "Reset Score" to "Reset All" in the template (two occurrences).

- [x] Task 2: Add Timer to Medic Overlay
  - [x] Modify `kurashScoreBoard.vue` to add a timer element inside the `v-if="isMedicMode"` overlay.
  - [x] Display `formatTime(time)` within this overlay.
  - [x] Style the timer to be large and visible (e.g., matching the "MEDIC" text style or slightly smaller below it).
