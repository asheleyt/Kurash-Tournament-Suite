# Tasks

- [x] Task 1: Fix Referee Controller scroll overflow
  - [x] Update `refereeController.vue` root container classes to conditionally apply `justify-center` based on `isSettingsOpen` state.
  - [x] Verify that `scrollTop = 0` works correctly with the new layout (no longer fighting against `justify-center`).
