# Tasks

- [x] Task 1: Update Referee Controller Initialization
  - [x] Update `refereeController.vue`: Initialize `gameState.gender` to `''`.
  - [x] Verify `gameState.time` is initialized to `0` (from previous task, but ensure consistency).

- [x] Task 2: Update Match Settings UI
  - [x] Verify Radio Buttons in `refereeController.vue` handle the empty string value correctly (neither checked).

- [x] Task 3: Update Timer Controls UI
  - [x] Verify "Male (4:00)" and "Female (3:00)" buttons use conditional styling that handles the empty state (both gray/inactive).
