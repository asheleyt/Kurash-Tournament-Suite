# Tasks

- [x] Task 1: Referee Controller Layout Tuning
  - [x] SubTask 1.1: Update `refereeController.vue` main wrapper to `flex flex-col justify-center` (or `h-full` with `my-auto`) to center content vertically.
  - [x] SubTask 1.2: Slightly increase gaps (`gap-2` -> `gap-4`) and padding (`p-2` -> `p-4`) to make it less cramped while keeping `h-screen` and `overflow-hidden`.

- [x] Task 2: Score Button Fixes
  - [x] SubTask 2.1: Refactor `ScoreButton` component in `refereeController.vue` to remove absolute positioning on label.
  - [x] SubTask 2.2: Use `flex flex-col items-center justify-center gap-1` for the button content layout.
  - [x] SubTask 2.3: Remove `mt-6` or any margins that were forcing the number down unnecessarily.

# Task Dependencies
- Tasks are independent.
