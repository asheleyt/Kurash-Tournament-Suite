# Tasks

- [x] Task 1: Implement weight input validation in `refereeController.vue`.
    - Add a check for non-numeric values.
    - Add a visual error state (e.g., red border/text) when input is invalid.
- [x] Task 2: Update `kurashScoreBoard.vue` penalty buttons.
    - Ensure the 'D' button (and others) does **not** have `opacity-50` or `opacity-40` classes when disabled.
    - Verify they remain fully opaque.
