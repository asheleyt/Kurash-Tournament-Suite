# Tasks

- [x] Task 1: Update button labels in `refereeController.vue` ("Reset" -> "Reset Timer", "Save Input" -> "Update Match").
- [x] Task 2: Implement confirmation dialogs for `handleResetAll` and `handleResetTime`.
- [x] Task 3: Verify visual feedback for disabled penalty buttons (ensure opacity is sufficient).
