# Tasks
- [x] Task 1: Update the class binding for Player 1 & 2 Name inputs in `refereeController.vue` to only apply `border-red-500` if `name !== '' && !isValidName(name)`.
- [x] Task 2: Update the `v-if` condition for the error message `<span>` to only show if `name !== '' && !isValidName(name)`.
