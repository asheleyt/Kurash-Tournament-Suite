# Tasks

- [x] Task 1: Implement Dynamic Scrolling
  - [x] SubTask 1.1: Modify the root `div` in `refereeController.vue` to bind its class.
  - [x] SubTask 1.2: Apply `overflow-y-auto` when `isSettingsOpen` is true, and `overflow-hidden` when false.

- [x] Task 2: Fix Button Heights
  - [x] SubTask 2.1: Update `ScoreButton` component height from `h-20` to `h-24` (and verify flex centering).
  - [x] SubTask 2.2: Locate the "Reset" button (Timer Controls section) and change its class from `h-16` to `h-12`.

# Task Dependencies
- Tasks are independent.
